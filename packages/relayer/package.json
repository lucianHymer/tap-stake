{
  "name": "@tap-stake/relayer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "format": "biome format --write src/",
    "lint": "biome lint src/",
    "check": "biome check src/",
    "check:write": "biome check --write src/"
  },
  "dependencies": {
    "viem": "^2.37.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@vitest/ui": "^4.0.6",
    "typescript": "^5.8.3",
    "vitest": "^4.0.6",
    "wrangler": "^3.101.0"
  }
}
